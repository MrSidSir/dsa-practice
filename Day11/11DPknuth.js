/*
🔧 Problem: Optimize DP[i][j] = min_{k} (DP[i][k] + DP[k][j]) + C[i][j]
📝 Key Point: Reduces from O(n^3) to O(n^2) if quadrangle inequality holds.
⚠️ Used in optimal matrix chain multiplication, merge cost minimization.
*/

// Omitted proof-based implementation here; let me know for full explanation in next if required.
